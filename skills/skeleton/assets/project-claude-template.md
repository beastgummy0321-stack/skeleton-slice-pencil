# {{專案名}} — 專案規範（/skeleton 生成）

## 開場（每次 session）

1. 先讀 `BOARD.md`。有 `[>]` 行 → 主動報：「上次走到＜片名＞的第＜N＞站（＜卡點＞），
   要繼續嗎？」使用者答「繼續」→ 進 /slice 續跑。
2. 使用者提任何功能／修改需求 → 走 /slice 入口分流，不得跳過。

## 地標

- 技術棧：{{stack 一行}}
- 資料形狀（合約）：`src/schemas/`。schema 即前後端唯一合約。
- 模組：`src/modules/<名>/`，對外唯一進入點 `index.ts`。
  跨模組 import 非進入點檔一律禁止（depcruise 擋）。
- 假資料：只放 `src/mocks/`，單一入口。`if (mock)` 分支一律禁止。
- 看板：`BOARD.md` 只放未開始＋進行中。完成即刪行；歷史查 git log。

## 鐵律

- schema 凍結後改動＝回 /slice 第①站走變更流程，偷改視為未完成。
- 接真後端只有一個動作：移除假資料源。前端跟著改＝未完成。
- lint／型別／測試紅字不得繞過、不得 skip。
- C 路線的實作者不得自審；審查由獨立 agent 執行。A／B 路線機器閘門綠即完成，不另派審查。
- `HANDOFF-*.md` 讀完即轉成看板行或票，同一 session 內刪除；「現在做到哪」只活在 `BOARD.md`，寫第二份必漂移。
- 切片過程檔（UNKNOWN 清單、草稿）收工即刪。

## 對使用者說話

使用者非工程背景：白話、一次一題、給選項＋標推薦、時間講具體。術語必附一句比喻。
